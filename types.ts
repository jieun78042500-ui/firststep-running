export type WeatherRunningStatus='달리기 좋은 날'|'주의해서 달리기'|'실내 운동 추천';
export interface UserProfile{name:string;region:string;level:string;allergies?:string} export interface WeatherData{region:string;temperature:number;feelsLike:number;rain:number;humidity:number;wind:number;dust:string;uv:number;status:WeatherRunningStatus} export interface WorkoutPlan{id:string;date:string;title:string;distance:number;tasks:string[]} export interface WorkoutRecord{id:string;date:string;type:string;distance:number;minutes:number;pace:string;intensity:string;condition:string;memo?:string} export interface ExerciseGuide{id:string;name:string;purpose:string;reps:string;level:string;body:string;caution:string;mistake:string} export interface MealPlan{id:string;timing:string;title:string;foods:string[];carbs:number;protein:number;fat:number;water:number} export interface MarathonRace{id:string;name:string;date:string;region:string;distances:string[];registration:string;status:string;beginner:boolean;officialUrl?:string} export interface RunningGoal{weeklyKm:number;weeklyCount:number;targetRace?:string} export interface FavoriteRace{raceId:string;savedAt:string} export interface NotificationSetting{workout:boolean;race:boolean;weather:boolean}
